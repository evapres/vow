import type { Metadata } from "next";
import { Noto_Serif_Display, Qwitcher_Grypen, Syne } from "next/font/google";
import "./globals.css";

const bodyFont = Syne({
  variable: "--font-body",
  subsets: ["latin"],
});

const serifFont = Noto_Serif_Display({
  variable: "--font-heading",
  weight: "400",
  subsets: ["latin"],
});

const specialFont = Qwitcher_Grypen({
  variable: "--font-special",
  weight: "400",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${bodyFont.variable} ${serifFont.variable} ${specialFont.variable} h-full min-h-full antialiased`}
      style={{
        backgroundColor: "#FCFCF6",
      }}
    >
      <body className="flex min-h-full flex-col bg-transparent font-sans">{children}</body>
    </html>
  );
}
